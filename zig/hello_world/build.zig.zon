.{
    .name = .zzdds_examples_hello_world_zig,
    .version = "0.0.0",
    .fingerprint = 0xc6ec662f065c5414,
    .minimum_zig_version = "0.16.0",
    .dependencies = .{
        // Same convention as zig/shape: point straight at the sibling
        // checkout this workspace already assumes exists, rather than a
        // tagged release URL -- see zig/shape/build.zig.zon's comment.
        //
        // No direct dependency on zidl here -- build.zig gets the zidl
        // executable and zidl_rt module *through* zzdds instead (which
        // re-exposes both), so there's only ever one zidl resolution in the
        // whole graph. See build.zig's comment for why a second, independent
        // one breaks the build outright when zidl is a `.path` dependency.
        .zzdds = .{
            .path = "../../../zzdds",
        },
    },
    .paths = .{
        "build.zig",
        "build.zig.zon",
        "publisher.zig",
        "subscriber.zig",
        "idl",
    },
}
